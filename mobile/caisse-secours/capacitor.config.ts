import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.caissesecours.collecte',
  appName: 'Caisse Secours Collecte',
  webDir: 'dist'
};

export default config;
